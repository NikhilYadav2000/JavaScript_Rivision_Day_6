const os=require("os"); // as it's a builtin function we only need to write its name
console.log(os.freemem()); // just run the code
console.log(os.homedir());
console.log(os.hostname());
console.log(os.platform());
console.log(os.release());
console.log(os.type());
/*
9286356992
C:\Users\dac
dac-39
win32
10.0.22631
Windows_NT
*/